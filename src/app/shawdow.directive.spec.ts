import { ShawdowDirective } from './shawdow.directive';

describe('ShawdowDirective', () => {
  it('should create an instance', () => {
    const directive = new ShawdowDirective();
    expect(directive).toBeTruthy();
  });
});
